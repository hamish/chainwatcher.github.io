Chainwatcher - View bitcoin transactions in realtime

To install dependencies:
# install npm, then...
sudo npm install -g bower-installer
npm install connect serve-static

cd chainwatcher.github.io
bower-installer

node runServer.js

